<h1>New Verification</h1>

<%= form_with(model: [@employee, @verification], local: true) do |f| %>
  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <%= f.submit "Create Verification" %>
<% end %>
